{
  "name": "mini-rag-edge",
  "version": "1.0.0",
  "description": "Mini RAG on Cloudflare Workers (D1 + Vectorize + Workers AI)",
   "type": "module",
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "wrangler": "^4.38.0"
  }
}
